const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:8080', { waitUntil: 'networkidle0' }); const mutations = await page.evaluate(() => { return window.__mutations || 'No mutations recorded'; }); console.log('Mutations:', mutations); await browser.close(); })();
